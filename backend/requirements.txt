fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
greenlet==3.1.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.8
email-validator==2.2.0
tenacity==9.0.0
gunicorn==23.0.0
slowapi==0.1.9
httpx==0.27.2
# ICE Copilot (AI-1) — LangChain 1.x harness, provider-agnostic model layer.
# OpenRouter is supported via the OpenAI-compatible path (ChatOpenAI + base_url),
# so no langchain-openrouter SDK is required (it would force pydantic>=2.11).
langchain==1.3.15
langchain-core==1.5.5
langchain-openai==1.5.1
langchain-anthropic==1.5.6
langchain-groq==1.1.3
openai==2.54.0
# LangGraph family — pinned to the exact set langchain 1.3.15 resolves to.
# AI-1 relies on ToolRuntime context injection, CONFIG_KEY_RUNTIME and
# stream_mode=["messages","updates"], which are version-sensitive. Host test
# environment and Docker runtime MUST resolve to this same set.
langgraph==1.2.11
langgraph-prebuilt==1.1.0
langgraph-checkpoint==4.2.0
langgraph-sdk==0.4.2
